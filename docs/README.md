# MPC Plus Documentation

MPC Plus CLI 的 Fumadocs 文档站，已接入仓库根 workspace。

```bash
vp install
vp run docs:dev
```

打开 <http://localhost:3000> 查看文档。内容位于 `content/docs`，使用 MDX 编写。

```bash
vp run docs:check
vp run docs:build
```
